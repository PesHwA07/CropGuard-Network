# Core
fastapi>=0.110.0
uvicorn[standard]>=0.27.0
pydantic-settings>=2.0.0
python-multipart>=0.0.6

# Database — PostgreSQL (async app + sync migrations)
sqlalchemy[asyncio]>=2.0.0
asyncpg>=0.29.0
psycopg2-binary>=2.9.0
alembic>=1.13.0

# Database — MongoDB
motor>=3.3.0

# Auth
python-jose[cryptography]>=3.3.0
passlib[bcrypt]>=1.7.0

# Kafka
aiokafka>=0.10.0

# Computer Vision (inference — training runs on Colab separately)
ultralytics>=8.1.0
opencv-python-headless>=4.9.0

# RAG
chromadb>=0.4.0
sentence-transformers>=2.5.0
groq>=0.4.0

# Experiment Tracking
mlflow>=2.14.0

# Testing
httpx>=0.26.0
pytest>=8.0.0
pytest-asyncio>=0.23.0
